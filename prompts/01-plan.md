I want to know which retrieval configuration our RAG app should ship with, and
whether the reranker is worth its latency.

Use the corpus-eval skill. Before doing any work, write TODO.md with the full
plan broken into checkable items — see TODO.template.md for the shape. Include
your estimate of how many endpoint calls the whole thing will take.

Then stop and show me the plan. Do not start executing.
