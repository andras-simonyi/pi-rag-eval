# Copy to .env and fill in. Never commit .env.
#
# Base URL of the running Gradio app from the RAG lab notebook.
# Changes every relaunch. No trailing slash.
CORPUS_URL="https://xxxxxxxxxxxx.gradio.live"

# Only needed if you point Pi at a paid provider rather than a subscription
# login. In Codespaces, prefer repository secrets over this file.
# ANTHROPIC_API_KEY=""
# OPENAI_API_KEY=""
